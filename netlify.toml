# Netlify configuration
# Proxy all API requests to EC2 backend

[[redirects]]
  from = "/api/*"
  to = "http://16.16.68.123:5000/api/:splat"
  status = 200
  force = true

# SPA fallback for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
